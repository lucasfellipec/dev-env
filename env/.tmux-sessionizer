#!/usr/bin/env fish

# i do this to every directory i am in
# 1 is neovim
# 2 is where i run commands
#
# could be cool!
# this means i could create a neovim plugin to shoot commands out to "scratch"
if test (pwd) = "$HOME/www/personal"
    clear
    return
end

tmux new-window -dn scratch
vim .
clear
